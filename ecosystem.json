{
  "apps": [{

    "name"  : "Starveller",
    "script": "bin/prod.js",

    "env_production": {
      NODE_ENV: "production"
    }

  }],

  "deploy": {
    "production": {
      "user"       : "pm2",
      "host"       : "bangular.io",
      "ref"        : "origin/master",
      "repo"       : "git@github.com:SIGSEV/Starveller.git",
      "path"       : "/var/www/starveller",
      "post-deploy": "pnpm i && npm run build && NODE_ENV=production NODE_PATH=$NODE_PATH:./src pm2 startOrRestart ecosystem.json --env production"
    }
  }
}
